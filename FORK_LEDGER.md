# Codex Fork Ledger

This file is the maintenance map for behavior carried by `fork` but not by the stabilized upstream baseline on `main`. It is not a changelog. Keep one row per maintained capability and update that row when the capability changes, moves, lands upstream, or is retired.

The intended branch relationship is:

```text
upstream/main
    └── main       upstream behavior plus stabilization fixes
          └── fork all maintained downstream behavior
```

Use `git log --reverse main..fork` to inspect the current fork stack. Exact commit subjects are the stable semantic anchors for each capability; unlike short SHAs, they do not need to be refreshed after a history rewrite. Stabilization commits already contained by `main` do not belong in this ledger.

Direct CLI/TUI use, including over a remote terminal or SSH, is the supported client workflow. This fork does not promise app-server wire compatibility with ChatGPT web or mobile clients that target a different upstream release schema.

## Classification

| Kind | Meaning |
| --- | --- |
| Compatibility | Restores or preserves a preferred behavior that upstream removed or changed. |
| Capability | Adds user-visible runtime behavior not present upstream. |
| Observability | Makes otherwise opaque runtime state inspectable or auditable. |
| Efficiency | Reduces model-context, rendering, storage, or interaction overhead. |
| Platform | Maintains support for a platform or transport needed by this fork. |
| Release | Identifies and packages the maintained fork. |

## Maintained Capabilities

| Capability | Kind | Purpose | Primary fork entrypoints | Required upstream seams | Commits |
| --- | --- | --- | --- | --- | --- |
| Compact `apply_patch` tool guidance | Efficiency | Keep the model-visible patch contract explicit while avoiding redundant per-session instruction tokens. | `codex-rs/core/src/tools/handlers/apply_patch_spec.rs` | Tool schema construction and schema tests | `fix(tools): shorten apply_patch description` |
| Complete repeated `apply_patch` history | Observability | Preserve every verified update hunk when one patch edits the same file repeatedly so inline summaries, transcript review, and persisted completion events do not show only the final section. | `codex-rs/apply-patch/src/invocation.rs`<br>`codex-rs/tui/src/diff_render.rs` | Apply-patch verification, structured change aggregation, TUI diff rendering | `fix(apply-patch): preserve repeated file update hunks` |
| Unlabelled compact rate-limit percentages | Compatibility | Preserve the compact rate-limit presentation instead of adding repeated window labels to status surfaces. | `codex-rs/tui/src/chatwidget/status_controls.rs`<br>`codex-rs/tui/src/bottom_pane/status_line_setup.rs` | TUI status controls and snapshots | `Revert "tui: label compact rate-limit percentages (PR 24314)"` |
| `/approvals` compatibility command | Compatibility | Retain the familiar permissions command alongside `/permissions` without conflicting with the separate `/approve` auto-review action. | `codex-rs/tui/src/slash_command.rs`<br>`codex-rs/tui/src/chatwidget/slash_dispatch.rs` | TUI slash-command catalog and dispatch | `fix(tui): restore approvals slash command` |
| On-demand thread naming | Compatibility | Keep root and subagent threads unnamed until the user invokes `/rename`, avoiding hidden title-generation turns and unwanted persisted names while retaining an editable conversation-based suggestion in the explicit rename prompt. | `codex-rs/tui/src/app/thread_title.rs`<br>`codex-rs/tui/src/app/thread_routing.rs`<br>`codex-rs/tui/src/chatwidget/interaction.rs` | App-server thread-name notifications, hidden structured title requests, TUI slash-command dispatch | `fix(tui): make thread naming on demand` |
| In-place prompt editing by default | Compatibility | Roll a thread back when editing an earlier prompt, retaining the selected canonical turn/item identity across compaction, SQLite projection rebuilds, pagination, and resume; reuse loaded turn state instead of reparsing complete non-paginated rollouts, while leaving source-preserving prompt forks available through `fork_prompt_edits`. | `codex-rs/tui/src/app_backtrack.rs`<br>`codex-rs/tui/src/app/thread_turn_materialization.rs`<br>`codex-rs/tui/src/app/thread_routing.rs`<br>`codex-rs/core/src/session/handlers.rs`<br>`codex-rs/protocol/src/rollout.rs`<br>`codex-rs/thread-store/src/local/thread_history_materialization.rs` | App-server rollback, durable rollout markers, paginated thread-history projection, TUI thread/event lifecycle, feature configuration | `fix(tui): restore in-place prompt editing by default`, `fix(rollback): preserve exact durable thread boundaries` |
| Deferred tool discovery in Lite and Code Mode | Capability | Keep `tool_search` reachable when V1 tools are deferred by Responses Lite and expose ranked deferred MCP discovery inside Code Mode without injecting every schema up front. | `codex-rs/core/src/client.rs`<br>`codex-rs/core/src/tools/handlers/tool_search.rs`<br>`codex-rs/tools/src/code_mode.rs` | Responses request serialization, tool routing, Code Mode declarations/runtime | `fix(client): keep Lite tool_search reachable for deferred V1 agents`, `fix(code-mode): expose ranked deferred tool discovery` |
| Non-persistent raw response traces | Efficiency | Preserve opt-in transport diagnostics while excluding full Responses API payloads from the SQLite log sink to reduce sensitive-data retention and insert/prune churn. | `codex-rs/codex-api/src/sse/responses.rs`<br>`codex-rs/codex-api/src/endpoint/responses_websocket.rs` | State log filtering | `fix(state): stop persisting raw response payloads` |
| Nested certificate fallback classification | Compatibility | Recognize certificate-validation failures wrapped by transport clients so route-aware requests can select the configured TLS fallback without inspecting URL-bearing request-level error text. | `codex-rs/http-client/src/route_aware_client_pool.rs` | Route-aware client error classification and TLS fallback | `fix(http-client): classify nested certificate failures as TLS errors` |
| Complete role, model, and service-tier selection for subagents | Capability | Expose built-in roles without requiring custom role configuration; allow role-local MCP specialization, history-fork authorization, and non-empty base-instruction files resolved relative to the role config while keeping role developer instructions separate; treat configured role model/reasoning/tier values as defaults that explicit V1, V2, and user-authored spawn values may override; resolve each child's tier independently with its direct parent only as a fallback; persist those resolved settings across V1/V2 resume and eviction; allow MultiAgent V2 to spawn any loaded catalog model regardless of its default multi-agent tag; keep collaboration tools available to those V2 children; keep model selection independent from history inheritance; and keep delegation policy and arbitrarily large custom guidance under user configuration rather than model-catalog authority. | `codex-rs/core/src/agent/role.rs`<br>`codex-rs/core/src/agent/control/spawn.rs`<br>`codex-rs/core/src/agent/user_control/spawn.rs`<br>`codex-rs/core/src/session/multi_agents.rs`<br>`codex-rs/core/src/context/world_state/multi_agent_mode.rs`<br>`codex-rs/core/src/tools/handlers/multi_agents_common.rs`<br>`codex-rs/core/src/tools/handlers/multi_agents/spawn.rs`<br>`codex-rs/core/src/tools/handlers/multi_agents_v2/spawn.rs`<br>`codex-rs/core/src/tools/spec_plan.rs`<br>`codex-rs/thread-store/src/types.rs` | Spawn tool schemas, model catalog validation, child tool exposure, MCP requirements, history inheritance, durable thread metadata and cold resume, TUI app-server session configuration | `fix(multi-agent): expose built-in roles in spawn tools`, `fix(multi-agent): allow v2 model overrides across catalog tags`, `feat(multi-agent): default v2 spawn forks to no context`, `fix(multi-agent): keep delegation policy user-controlled`, `fix(multi-agent): preserve per-agent service tiers`, `fix(multi-agent): honor role base instruction files` |
| Interactive and resumable subagent threads | Compatibility | Keep parent-owned and V2 subagent threads directly inspectable and interactive, revive replayed or cold-resumed children through their owning control plane, and preserve routing, identity, completion delivery, graph state, and parent-return behavior. | `codex-rs/core/src/agent/control/spawn.rs`<br>`codex-rs/core/src/thread_manager/v2_spawn_resume.rs`<br>`codex-rs/tui/src/app/thread_resume.rs`<br>`codex-rs/tui/src/app/agent_navigation.rs` | App-server direct input, agent ownership and registry state, V1 adoption, V2 graph restoration, TUI thread navigation | `Revert "Make parent-owned sub-agent threads read-only in the TUI (PR 33841)"`, `Revert "app-server: reject direct input to multi-agent v2 sub-agents (PR 27173)"`, `feat(multi-agent): restore closed agents through their owning control plane` |
| Configurable and auditable V2 agent messaging | Observability | Select encrypted, encrypted-with-audit, or plaintext delivery; retain complete readable assignments where permitted; and preserve readable communication in rollout traces without confusing audit text with transport correlation. | `codex-rs/core/src/agent_communication.rs`<br>`codex-rs/core/src/tools/handlers/multi_agents_v2/`<br>`codex-rs/rollout-trace/src/reducer/tool/agents.rs` | Thread config locking, agent registry/control, spawn/send/follow-up schemas, rollout reduction | `feat(config): select multi-agent message delivery`, `feat(multi-agent): support auditable v2 message delivery`, `feat(rollout-trace): retain readable agent communication` |
| User-authorized context inheritance for agent spawns | Efficiency | Start model-authored V1 and V2 subagents without parent history by default, require `agents.allow_history_forks` globally or in the selected role config before a model may request inherited turns, and leave explicit user `/agent ... fork:` commands authoritative. V2 still supports `none`, `all`, or a bounded turn count through `features.multi_agent_v2.default_fork_turns`; authorized history forks preserve parent audit records while excluding stale delivery envelopes from child instructions. | `codex-rs/config/src/config_toml.rs`<br>`codex-rs/core/src/tools/handlers/multi_agents/spawn.rs`<br>`codex-rs/core/src/tools/handlers/multi_agents_v2/spawn.rs`<br>`codex-rs/core/src/agent/control/spawn.rs` | Agent configuration, spawn schemas and parsing, role config layering, fork-history filtering, user agent control | `feat(multi-agent): default v2 spawn forks to no context`, `fix(multi-agent): keep delegation policy user-controlled` |
| Auditable multi-agent transcripts | Observability | Show useful multi-line task prompts and completed wait responses with independent configurable caps, preserve readable V2 task messages in parent and child histories, surface attributable inter-agent communication through live and resumed transcripts without exposing encrypted content, and resolve collab labels from thread-wide canonical nickname/role metadata across paginated pages and complete exports. | `codex-rs/tui/src/multi_agents.rs`<br>`codex-rs/tui/src/thread_transcript.rs`<br>`codex-rs/tui/src/app/history_pagination.rs`<br>`codex-rs/tui/src/app/thread_routing.rs`<br>`codex-rs/tui/src/chatwidget/tool_lifecycle.rs`<br>`codex-rs/app-server-protocol/src/protocol/v2/item.rs`<br>`codex-rs/app-server/src/request_processors/response_item_transcript.rs`<br>`codex-rs/thread-store/src/local/thread_history_materialization.rs` | Canonical collab item conversion, TUI tool-history rendering and receiver cache, app-server thread history and notifications, durable rollout reconstruction, fork-owned history projections | `feat(multi-agent): configure agent prompt and response previews`, `fix(multi-agent): show v2 task messages in transcripts`, `feat(multi-agent): surface v2 inter-agent messages in thread transcripts` |
| Durable background subagent completions | Observability | Show terminal v1 and v2 child results immediately when no active wait owns presentation, retain their full model-visible context and canonical transcript rows through cancellation, rollback, shutdown, compaction, pagination, and cold resume, and allow a later explicit wait to render independently. | `codex-rs/core/src/session/sub_agent_completion.rs`<br>`codex-rs/core/src/agent/control/presentation.rs`<br>`codex-rs/protocol/src/sub_agent_completion.rs`<br>`codex-rs/thread-store/src/completion_artifacts.rs`<br>`codex-rs/tui/src/multi_agents/background_completion.rs` | Agent status publication, parent Session lifecycle, rollout persistence and reconstruction, app-server history projection, TUI collab rendering | `feat(multi-agent): surface durable background completions` |
| Event-driven subagent response observation | Capability | Let V1 lifecycle calls request the first complete commentary, subscribe to an exact target turn's final reply, or explicitly avoid a new final subscription through compact `w` flags; attribute automatic completion-wake turns distinctly from user turns; and deduplicate waits and multiple observers across rollback, compaction, recovery, close, migration, and V1/V2 control-plane boundaries. | `codex-rs/core/src/tools/handlers/multi_agents_spec.rs`<br>`codex-rs/core/src/agent/control/response_observer.rs`<br>`codex-rs/core/src/agent/control/response_delivery.rs`<br>`codex-rs/core/src/session/handlers.rs`<br>`codex-rs/core/src/session/response_observation.rs`<br>`codex-rs/thread-store/src/local/rollout_migration/canonicalizer.rs`<br>`docs/multi-agent-v1-response-observation.md` | Agent lifecycle tool schemas, exact-turn admission, durable rollout context, completion publication, Responses turn-trigger metadata, rollout migration, TUI commentary/final presentation | `feat(multi-agent): add durable response observation policies`, `fix(turns): attribute automatic wake turns` |
| Native user agent control plane | Capability | Let users inspect, spawn, prompt, queue, interrupt, resume or adopt, close, and change one-turn response observation for V1 and V2 agents directly through `/agent`, using durable root-scoped refs and nicknames while preserving canonical UUID ownership, structured user input, exact-turn delivery, source-side audit history, transcript inspection, explicit fork modes, and per-spawn model/reasoning overrides with catalog autocomplete. Keep the released alias migration in a high fork-owned version namespace and checksum-remap its original version 49 so upstream migration numbering remains compatible. | `codex-rs/core/src/agent/user_control/`<br>`codex-rs/state/src/runtime/agent_aliases/`<br>`codex-rs/app-server/src/request_processors/thread_processor/agent_control.rs`<br>`codex-rs/tui/src/chatwidget/agent_command.rs`<br>`codex-rs/tui/src/app/agent_control_pane.rs`<br>`docs/tui-agent-control.md` | Agent graph persistence and transfer, app-server v2 control APIs, response observation, fork-history projection, TUI composer and thread routing | `feat(multi-agent): add user-controlled agent dispatch`, `feat(multi-agent): add scoped replies and queued turns` |
| Thread-local hard deletion | Compatibility | Make `/delete` and `thread/delete` remove only the selected thread. Preserve spawned, adopted, and communicating threads together with their durable graph relationships so later research, inspection, resume, and transfer workflows remain available; reject deletion instead of cascading when another paginated thread physically depends on the selected history. | `codex-rs/app-server/src/request_processors/thread_delete.rs`<br>`codex-rs/state/src/runtime/threads.rs`<br>`codex-rs/tui/src/chatwidget/slash_dispatch.rs` | Thread-store reference checks, app-server lifecycle teardown, state graph persistence, TUI confirmation copy | `fix(delete): preserve related agent threads` |
| Visible automated approval decisions | Observability | Keep successful Guardian assessments visible in live and reconstructed TUI history so reviewed-and-approved actions remain distinguishable from actions that were never reviewed. | `codex-rs/tui/src/chatwidget/tool_requests.rs`<br>`codex-rs/tui/src/history_cell/approvals.rs` | Guardian assessment notifications, TUI history cells and snapshots | `revert(tui): keep approved Guardian assessments visible` |
| Complete code-mode call history | Observability | Show the complete code-mode invocation and result in ordinary TUI history as well as the full transcript instead of replacing successful calls with title-only summaries. | `codex-rs/tui/src/history_cell/mcp.rs` | MCP/code-mode history rendering and transcript projection | `revert(tui): keep complete code mode calls in history` |
| Visible and bounded local compaction | Observability | Render the compacted prompt in TUI, exec, app-server, and JSONL output; bound compaction time/output; and append session metadata that points later turns toward full rollout history. | `codex-rs/core/src/compact.rs`<br>`codex-rs/core/src/tasks/compact.rs`<br>`codex-rs/exec/src/exec_events.rs` | Core session/turn lifecycle, protocol items, app-server thread history, TUI rendering | `feat(compact): show compacted prompt output`, `feat(core): cap compaction output and add session metadata` |
| User-controlled context automation | Compatibility | Keep token budgeting, private context management, and automatic TUI recaps disabled until the user explicitly enables them; prevent model-catalog metadata from activating hidden work; retain manual `/recap`; and keep the visible `update_plan` tool available unless explicitly disabled. | `codex-rs/core/src/session/token_budget.rs`<br>`codex-rs/core/src/config/mod.rs`<br>`codex-rs/tui/src/app/recap.rs` | Model-catalog defaults, feature/config resolution, world-state instruction filtering, TUI recap scheduling | `fix(context): preserve user-controlled automation defaults` |
| Inspectable remote compaction handoff | Observability | Decode the provider-opaque post-compaction handoff through a locked-down helper, retry a configurable fallback decoder when the primary model is unavailable, show a live decode phase and the installed skill inventory, persist server-reported summary token counts without changing replacement history, and retain auditable agent progress and completions within the bounded retained-history budget; the user-controlled remote-compaction feature gate remains authoritative even for provider-capable models. | `codex-rs/core/src/compact_handoff_summary.rs`<br>`codex-rs/core/src/compact_skills_inventory.rs`<br>`codex-rs/core/src/compact_remote.rs`<br>`codex-rs/core/src/compact_remote_v2.rs`<br>`codex-rs/core/src/tasks/compact.rs` | Agent delegation, compaction protocol/events, rollout reconstruction, app-server notifications, TUI status | `feat(compact): decode remote compaction handoff`, `feat(compact): show remote handoff decode status`, `feat(compact): persist remote compaction token counts` |
| Compacted-media retention and vacuuming | Efficiency | Remove obsolete inline images and structured tool-output media from compacted model history instead of exposing a redundant retained-image budget, while retaining bounded, reopenable local-image provenance for one compaction window and providing crash-safe repair and vacuum paths for existing rollouts. | `codex-rs/core/src/context/compacted_media.rs`<br>`codex-rs/core/src/session/compacted_media_repair.rs`<br>`codex-rs/rollout/src/media_vacuum.rs` | Compaction replacement history, rollout reconstruction and rollback, thread-history projections, plain and compressed rollout storage | `fix(compaction): remove retained inline media from compacted history` |
| Explicit MCP prompt invocation policy | Capability | Keep configured MCP servers live, deferred, searchable, and callable while hiding selected inventories from direct context until `/mcp use` or `thread/mcpServer/activate` explicitly contributes them. | `codex-rs/core/src/context/mcp_server_use_instructions.rs`<br>`codex-rs/core/src/mcp_tool_exposure.rs`<br>`codex-rs/app-server/src/request_processors/mcp_processor.rs` | MCP catalog/runtime, config editing, session input ordering, compaction, TUI commands, app-server protocol and SDK | `feat(mcp): add explicit MCP prompt invocation policy` |
| Cross-home and path-based session forks | Capability | Fork saved or archived rollouts from another Codex home into the active home, copying complete effective paginated lineage into a self-contained destination while leaving the source read-only and ordinary in-home forks coordinated through their existing lifecycle. | `codex-rs/cli/src/main.rs`<br>`codex-rs/app-server/src/request_processors/thread_processor.rs`<br>`codex-rs/thread-store/src/local/fork_copy.rs`<br>`codex-rs/tui/src/cli.rs` | TUI startup/resume picker, app-server `thread/fork`, rollout lineage/rollback/compression, source-store snapshots | `feat(cli): fork sessions from alternate Codex homes`, `fix(fork): copy paginated lineage across Codex homes` |
| Durable promoted skills and goal context | Capability | Keep explicitly selected skills discoverable across compaction and resume, present the current post-compaction inventory without implying that transient resolution failures erased durable promotion, reconstruct only current active-goal context, anchor steering to sources authoritative for the objective, prevent ordinary forks/subagents from inheriting goal authority, and tolerate transient user-side Git breakage in configured skill directories. | `codex-rs/ext/goal/src/runtime/objective_projection.rs`<br>`codex-rs/ext/goal/templates/goals/`<br>`codex-rs/ext/skills/src/selection.rs`<br>`codex-rs/core/src/compact_skills_inventory.rs`<br>`codex-rs/core/src/session/inject.rs`<br>`codex-rs/prompts/templates/goals/` | Skills inventory/provider lifecycle, compaction installation, app-server goal/fork APIs, state goal storage, extension contribution ordering | `feat(goals,skills): preserve promoted context across compaction and forks`, `fix(goal): preserve objective authority during steering`, `feat(compact): decode remote compaction handoff` |
| Compact discovered skill paths | Efficiency | Preserve canonical skill identity internally while rendering user-facing discovery routes such as `~/.agents/skills/...` instead of resolved checkout paths and repeated home-directory prefixes. | `codex-rs/ext/skills/src/loader/host.rs`<br>`codex-rs/ext/skills/src/provider/host.rs` | Host skill discovery and model-visible inventory rendering | `fix(skills): render compact discovered host paths` |
| User-controlled skill catalog budget | Capability | Keep the context-window-derived default for available-skill metadata while honoring an explicitly configured `skills.max_context_tokens` value without an OpenAI-model-specific ceiling. | `codex-rs/config/src/skills_config.rs`<br>`codex-rs/ext/skills/src/render.rs` | Skills configuration/schema and model-visible catalog rendering | `revert(skills): keep configured catalog budgets uncapped` |
| Named early skill-read activity | Observability | Attribute `SKILL.md` reads to the selected skill even when the TUI receives the tool call before asynchronous skill metadata has populated ChatWidget state. | `codex-rs/tui/src/chatwidget/skills.rs` | TUI tool-call classification and transcript rendering | `fix(tui): retain skill names on early SKILL.md reads` |
| Realtime TUI voice and native WebRTC | Platform | Retain the removed realtime voice UI and standalone native WebRTC integration, then extend audio-session lifecycle and rendering support to Linux GNU builds using API-key authentication. | `codex-rs/realtime-webrtc/`<br>`codex-rs/tui/src/chatwidget/realtime.rs`<br>`codex-rs/tui/src/audio_device.rs`<br>`codex-rs/tui/src/voice.rs` | Cargo/Bazel WebRTC targets, TUI app events, composer, settings, session routing, audio dependencies and release workflows | `revert(realtime): restore TUI voice and native WebRTC support`, `feat(tui): enable linux gnu realtime audio in chat sessions` |
| ChatGPT OAuth dictation | Capability | Record composer dictation, split long recordings with VAD, transcribe chunks through the ChatGPT endpoint over one reusable route-aware HTTP pool, and append ordered transcripts without imposing a dictation-specific text cap. | `codex-rs/tui/src/chatwidget/dictation.rs`<br>`codex-rs/tui/src/voice/chunked.rs`<br>`codex-rs/tui/src/voice/transcription.rs` | ChatGPT authentication, shared HTTP routing and Cloudflare-cookie policy, configurable keymaps, composer lifecycle, audio dependencies | `feat(tui): add ChatGPT OAuth dictation shortcut` |
| Native audio transcript markers | Observability | Keep upstream native model-audio inputs distinct from editable OAuth dictation and render an explicit `[audio]` marker for every attachment in live, paginated, resumed, and exported TUI transcripts instead of silently omitting it. | `codex-rs/tui/src/chatwidget/user_messages.rs`<br>`codex-rs/tui/src/thread_transcript.rs` | App-server user-message projection, pending-steer matching, transcript hydration and export | `fix(tui): retain native audio attachments in transcripts` |
| Configurable shell and unified-exec timing | Capability | Configure omitted shell timeouts, initial/background yield windows, and empty-poll caps; let `!`/`/shell` commands run as detached background work until exit or explicit cancellation by default while retaining opt-in deadlines, exposing every live process ID through `/ps`, completing those IDs in `/stop`, and stopping one process through `/stop <id>` without blocking model turns or queued follow-ups. | `codex-rs/core/src/tools/handlers/unified_exec/`<br>`codex-rs/core/src/tools/handlers/shell/`<br>`codex-rs/core/src/tasks/user_shell.rs`<br>`codex-rs/tui/src/history_cell/exec.rs`<br>`codex-rs/tui/src/chatwidget/slash_dispatch.rs` | Config loading/locking, turn context, tool schemas, process manager, app-server background-terminal control, TUI command lifecycle | `feat(config): add default shell command timeout`, `feat(config): add unified exec default yield windows`, `feat(unified-exec): allow uncapped background polls`, `feat(user-shell): make command timeout configurable`, `feat(user-shell): add explicit long-running process control` |
| User-shell completion policies | Capability | Let user-authored `!` commands choose passive, completion-wake, or presentation-only result delivery through compact `w` flags; order selected commands behind every earlier user-shell submission without blocking ordinary turns; keep queued commands visible and cancellable through `/ps` and `/stop`; suppress goal idle work while a requested completion wake remains pending; and attribute a resulting automatic wake turn distinctly from user-authored work. | `codex-rs/core/src/tasks/user_shell.rs`<br>`codex-rs/core/src/session/inject.rs`<br>`codex-rs/core/src/unified_exec/user_shell_queue.rs`<br>`codex-rs/protocol/src/protocol.rs`<br>`codex-rs/app-server-protocol/src/protocol/v2/thread.rs`<br>`codex-rs/tui/src/user_shell_command.rs` | User-shell process lifecycle, active-turn steering and idle wake scheduling, Responses turn-trigger metadata, app-server v2 schema, TUI composer parsing and highlighting | `feat(user-shell): add queued completion policies`, `fix(turns): attribute automatic wake turns` |
| Fail-closed human command approval deadlines | Capability | Let unattended on-request command approvals expire against an optional core-authoritative monotonic deadline, reject late responses without executing the command, preserve untimed handling when either app-server timing field is absent, and keep Guardian, patch approvals, and omitted deadlines on their existing timing behavior. | `codex-rs/core/src/session/mod.rs`<br>`codex-rs/core/src/tools/approvals.rs`<br>`codex-rs/app-server-protocol/src/protocol/v2/item.rs`<br>`codex-rs/app-server/src/bespoke_event_handling.rs`<br>`codex-rs/mcp-server/src/exec_approval.rs`<br>`codex-rs/tui/src/bottom_pane/approval_overlay.rs` | Approval protocol and callback routing, delegated turn configuration, app-server/MCP response arbitration, TUI pending-request lifecycle | `feat(approvals): add fail-closed human command timeouts` |
| Lossless terminal execution, waits, and configurable previews | Observability | Show which terminal and command an empty poll checked, classify ordinary Python commands as executed rather than directory listings, preserve complete bounded output across initial-yield and process-exit races, recover offset-proven remote replay gaps, restore process-local terminals only for the actual active turn during thread switching, render every grouped command in normal live and resumed history, render live wait countdowns, and independently cap agent/tool versus user-shell previews. | `codex-rs/core/src/unified_exec/async_watcher.rs`<br>`codex-rs/core/src/unified_exec/head_tail_buffer.rs`<br>`codex-rs/exec-server/src/client_recovery.rs`<br>`codex-rs/shell-command/src/parse_command.rs`<br>`codex-rs/tui/src/chatwidget/command_lifecycle.rs`<br>`codex-rs/tui/src/exec_cell/render.rs` | Unified-exec process/output ownership, exec-server replay offsets, completed-process cache, app-server terminal notifications, TUI status/history/pager | `feat(tui): configure command output previews`, `feat(tui): show countdowns for background waits`, `feat(exec,tui): surface complete terminal output checks`, `fix(tui): render non-listing Python commands as executed`, `fix(unified-exec): make polling and terminal replay lossless` |
| Non-paginated durable history by default | Compatibility | Create durable TUI and exec sessions with complete non-paginated rollout history unless a client explicitly opts into experimental pagination, preserving the fork's audit-first storage and resume behavior. | `codex-rs/tui/src/app_server_session.rs`<br>`codex-rs/exec/src/lib.rs` | Thread start parameters, app-server history modes, resume and fork compatibility | `fix(history): default durable sessions to non-paginated rollouts` |
| Configurable diff backgrounds | Capability | Select adaptive, disabled, theme-derived, or custom diff backgrounds while preserving syntax highlighting and normalizing tabs before wrapping. | `codex-rs/tui/src/diff_render.rs`<br>`codex-rs/tui/src/render/highlight.rs` | TUI config/schema and startup theme resolution | `feat(tui): add configurable diff backgrounds and unify custom override keys` |
| Configurable desktop notification previews | Capability | Tune agent-turn, exec-approval, and user-input notification lengths instead of relying on hard-coded grapheme limits. | `codex-rs/tui/src/chatwidget/notifications.rs` | TUI config/schema and notification creation/display paths | `feat(tui): make desktop notification preview limits configurable` |
| Scalable transcript switching, review, and paging | Efficiency | Bound terminal scrollback replay when switching agents, render Ctrl+T transcript pages in proportion to the visible viewport, and default the pager to a concise chronological review with exact retained output one keypress away. | `codex-rs/tui/src/transcript_reflow.rs`<br>`codex-rs/tui/src/pager_overlay.rs`<br>`codex-rs/tui/src/pager_overlay/transcript.rs`<br>`codex-rs/tui/src/render/renderable.rs` | TUI thread switching, resize/reflow, history-cell representation and navigation, hyperlink mapping | `fix(tui): cap agent switch scrollback replay`, `feat(tui): virtualize transcript pager rendering`, `feat(tui): add concise transcript review navigation` |
| Fork release version | Release | Make fork binaries and runtime metadata report the upstream-aligned maintained release version while leaving feature commits reusable at the source workspace baseline and keeping version-bearing TUI snapshots stable across source and release builds. | `codex-rs/Cargo.toml`<br>`codex-rs/mcp-server/tests/common/mcp_process.rs`<br>`codex-rs/tui/src/version.rs` | Workspace package version, build-metadata assertions, and version-bearing TUI output/snapshots | `chore(fork): reset workspace version for reusable commits`, `chore(fork): set release version to 0.153.4` |

## Update Protocol

- Compare `main..fork` after every upstream rebase.
- Add a row when a new maintained behavior lands; amend an existing row when work extends the same capability.
- Update subject anchors only when a capability commit is reworded, split, or squashed.
- If upstream implements an equivalent capability, compare behavior and tests before removing the row. Record whether the carry was dropped, narrowed, or deliberately retained.
- Keep stabilization-only changes on `main` out of this ledger. If a change is required only to make upstream reliable and does not alter fork behavior, it belongs to the stabilized baseline.
- Keep release-only version changes at the fork tip so feature commits remain reusable and reviewable at the workspace baseline version.

## Review Checklist

Before declaring the ledger current:

1. `git log --reverse main..fork` has no unclassified behavior commit.
2. Every listed entrypoint still exists on `fork`.
3. Config-backed capabilities remain represented in `docs/config.md` and the generated config schema.
4. App-server API changes remain documented and represented in generated JSON/TypeScript schemas.
5. Resume, fork, rollout, and compaction changes remain compatible with existing persisted sessions.
6. Remote manual verification and Linux release workflows pass for the current fork tip.
